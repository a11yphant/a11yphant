mutation updateCodeLevelSubmission($submissionInput: UpdateCodeLevelSubmissionInput!) {
  updateCodeLevelSubmission(submissionInput: $submissionInput) {
    submission {
        id
        css
        html
        js
    }
  }
}
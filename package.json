{
  "name": "a11y-challenges",
  "version": "0.0.1",
  "description": "",
  "author": "Michael Brandstätter | Thomas Dax | Daniela Kubesch | Luca Pircher",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "postinstall": "npm --prefix services/api install && npm --prefix services/site install && npm --prefix services/submission-checker install",
    "dotenv": "dotenv",
    "lint": "npm --prefix services/api run lint && npm --prefix services/site run lint",
    "start": "npm run dev",
    "dev": "pm2 start pm2-ecosystem.config.js",
    "dev:no-daemon": "node pm2-start-no-daemon.js",
    "stop": "pm2 stop a11y-challenges && pm2 del a11y-challenges",
    "dev:api": "npm --prefix services/api install && npm --prefix services/api run start:dev",
    "dev:site": "npm --prefix services/site install && wait-on -l tcp:$API_PORT && npm --prefix services/site run dev",
    "dev:submission-checker": "npm --prefix services/submission-checker install && npm --prefix services/submission-checker run start:dev",
    "debug:api": "pm2 logs a11y-challenges-api",
    "debug:site": "pm2 logs a11y-challenges-site",
    "debug:submission-checker": "pm2 logs a11y-challenges-submission-checker"
  },
  "repository": {
    "type": "git",
    "url": "https://gitlab.mediacube.at/a11y-challenges/a11y-challenges.git"
  },
  "keywords": [],
  "devDependencies": {
    "dotenv-cli": "4.0.0",
    "husky": "4.3.8",
    "lint-staged": "10.5.3",
    "pm2": "4.5.2",
    "wait-on": "5.2.1"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "dependencies": {}
}

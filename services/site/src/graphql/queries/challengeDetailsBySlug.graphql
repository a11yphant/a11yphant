query challengeDetailsBySlug ($slug: String!) {
    challenge: challengeBySlug(slug: $slug) {
        id
        name
        introduction
        levels {
            id
            order
            status
        }
    }
}
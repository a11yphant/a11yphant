mutation updateSubmission($submissionInput: UpdateSubmissionInput!) {
  updateSubmission(submissionInput: $submissionInput) {
    submission {
        id
        css
        html
        js
    }
  }
}
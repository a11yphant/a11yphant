mutation createSubmission($submissionInput: CreateSubmissionInput!) {
  createSubmission(submissionInput: $submissionInput) {
    submission {
        id
        html
        css
        js
    }
  }
}
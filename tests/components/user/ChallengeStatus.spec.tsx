// HOW DOES THIS WORK AGAIN? AREN'T THE TESTS ALREADY ON THE PROFILE PAGE?

describe("ChallengeStatus", () => {
  it("renders the ChallengeStatus", () => {
    // render(<ChallengeStatus id="testID" challengeStatus={} challenges={} />);
    // expect(ChallengeStatusMock).toHaveBeenCalled();
  });
});
